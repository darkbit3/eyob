# Frontend — BidLow Customer App
# Dev: leave as localhost (Vite proxy handles /api → backend)
# Production (Render): set VITE_API_URL=https://eyob-backend.onrender.com in Render's env settings
VITE_API_URL=https://eyob-backend.onrender.com
